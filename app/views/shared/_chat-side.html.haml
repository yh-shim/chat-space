.chat-side
  .side-header
    .side-header__box
      %p.side-header__box__user-name
        = current_user.name
      %ul.side-header__box__wrapper
        %li.side-header__box__wrapper__create-group
          =link_to new_group_path, class: "icon" do
            = fa_icon 'users'
        %li.side-header__box__wrapper__edit-user
          =link_to edit_user_path(current_user), class: "icon" do
            = fa_icon 'cog'
  .side-groups
    .group
      - current_user.groups.each do |group|
        = link_to group_chats_path(group), class: "icon" do
          %p.group__name
            = group.name
          %p.group__message
            メッセージはまだありません。
